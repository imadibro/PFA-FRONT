import type { SystemMode } from '@core/types'

const OperationList = ({ mode }: { mode: SystemMode }) => {
  return <div className='flex bs-full justify-center'>Operation List</div>
}

export default OperationList
